pipeline {
    agent { 
		docker { 
			image 'maven:3.8.4-jdk-8-slim'
		}
	}
    stages {
        stage('tests') {
            steps {
                sh "mvn -version"
            }
        }
    }
}
